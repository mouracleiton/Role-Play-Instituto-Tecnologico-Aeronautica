{
  "name": "@ita-rp/curriculum",
  "version": "1.0.0",
  "description": "Carregamento e processamento de dados curriculares para ITA RP Game",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:run": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ita-rp/shared-types": "*",
    "vite": "7.2.6"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^4.0.14"
  }
}
